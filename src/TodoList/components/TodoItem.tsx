import React from 'react'
import { Todo as Todo } from '../todoList.types'

type Props = {
  todo: Todo
}

export const TodoItem = (props: Props) => {
  return (
    <div>
      <div></div>
      <div></div>
      <div></div>
    </div>
  )
}
